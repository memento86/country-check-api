nelmio_api_doc:
    documentation:
        info:
            title: Freepik Api
            description: Api Rest to evaluate different criterias of a country
            version: 1.0.0
    areas: # to filter documented areas
        path_patterns:
            - ^/api(?!/doc(\.json)?$) # Accepts routes under /api except /api/doc
